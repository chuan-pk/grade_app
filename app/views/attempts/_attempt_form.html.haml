%label year:
=select(:attempt, :year, (2014..Date.today.year))
%label semester:
=select(:attempt, :semester, [1, 2, 3])
%label grade:
=select(:attempt, :grade, ['A', 'B+', 'B', 'C+', 'C', 'D+', 'D', 'F'])